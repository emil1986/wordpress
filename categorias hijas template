< ?php
$current_category = single_cat_title("", false);
$cat = get_cat_ID($current_category);
$category = get_category_parents($cat, FALSE);
$category[0]- >cat_name;
if($current_category == 'Películas' || $category[0] == 'Películas') { ? >
    < ?php include('peliculas.php'); ? >
< ?php } else { ? >

Código del Archive.php ó category.php este empieza con < ?php get_header(); ? >
 y termina en < ?php get_footer(); ? >

< ?php } ? >
